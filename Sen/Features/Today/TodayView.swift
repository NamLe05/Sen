import SwiftUI

// TODO: Main screen — shows today's time blocks with lotus bloom visualization, check-off interactions

struct TodayView: View {
    @State private var viewModel = TodayViewModel()

    var body: some View {
        Text("Today")
    }
}
