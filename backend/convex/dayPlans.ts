import { query, mutation } from "./_generated/server";
import { v } from "convex/values";

// TODO: getTodayPlan — query today's plan for authenticated user using by_user_date index
// TODO: getPlansByDateRange — paginated query for calendar view
// TODO: checkOffBlock — mutation to toggle block checkedOff status (auth + authorize first)
// TODO: savePlan — mutation to store AI-generated plan (called from ai.ts internalAction)
