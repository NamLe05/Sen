import SwiftUI

// TODO: Planning screen — voice recording or quick form input, triggers Gemini AI plan generation

struct PlanView: View {
    @State private var viewModel = PlanViewModel()

    var body: some View {
        Text("Plan")
    }
}
