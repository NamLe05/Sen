import SwiftUI

// TODO: Calendar screen — monthly grid showing past days with completion status, tap to view day detail

struct CalendarView: View {
    @State private var viewModel = CalendarViewModel()

    var body: some View {
        Text("Calendar")
    }
}
